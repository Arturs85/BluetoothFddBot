<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">
        <!--android:actionLayout="@layout/menu_switch" -->

        <item
android:id="@+id/actionCheck"
            android:actionLayout="@layout/menu_switch"
android:icon="@android:drawable/checkbox_on_background"
            android:title="One Frame"
    app:showAsAction="always"/>


        <item
            android:id="@+id/action_add"
            android:icon="@android:drawable/ic_menu_save"
            android:title="Jauns"
            app:showAsAction="always"/>
        <item
            android:id="@+id/action_changeToOdo"
            android:icon="@android:drawable/ic_menu_mapmode"
            android:title="Odo"
            app:showAsAction="always"/>
        <item
            android:id="@+id/action_changeToOverlay"
            android:icon="@android:drawable/ic_menu_directions"
            android:title="Overlay"
            app:showAsAction="always"/>
        <item
            android:id="@+id/action_resetLocation"
            android:icon="@android:drawable/ic_menu_revert"
            android:title="reset"
            app:showAsAction="always"/>


</menu>